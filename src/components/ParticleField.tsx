// ParticleField.tsx - This component is a placeholder that returns null.
// The actual particle background functionality has been removed, but this
// file is kept to prevent breaking imports throughout the codebase.

"use client"

import { memo } from "react"

const ParticleField = memo(() => {
  // This component intentionally returns null as the particle background
  // functionality has been removed for performance reasons
  return null
})

ParticleField.displayName = "ParticleField"

export default ParticleField
